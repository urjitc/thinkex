# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_PORT=3000

# PostgreSQL Database Configuration
POSTGRES_USER=thinkex
POSTGRES_PASSWORD=thinkex_password_change_me
POSTGRES_DB=thinkex
POSTGRES_PORT=5432

# Database Connection (auto-generated from above, or set manually)
# DATABASE_URL=postgresql://thinkex:thinkex_password_change_me@postgres:5432/thinkex

# Better Auth (Authentication)
# Generate a secret: openssl rand -base64 32
BETTER_AUTH_SECRET=your-better-auth-secret-change-this
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth (for Better Auth)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Storage Configuration
# Options: 'supabase' or 'local'
STORAGE_TYPE=local

# Option 1: Local File Storage (Recommended for self-hosting)
# Files are stored in ./uploads directory (mounted as volume in Docker)
# No additional configuration needed - just set STORAGE_TYPE=local

# Option 2: Supabase Storage (Cloud-based)
# Uncomment and configure if using Supabase
# STORAGE_TYPE=supabase
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=eyJ...
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Google AI (Gemini)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=AIza...

# Assistant UI (optional)
NEXT_PUBLIC_ASSISTANT_BASE_URL=https://...
ASSISTANT_API_KEY=sk_aui_...
